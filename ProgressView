[21.11.25, 10:30:32] Jil: import { Book } from '../types';
import { ArrowLeft, BookOpen, TrendingUp, Award } from 'lucide-react';
import { BarChart, Bar, XAxis, YAxis, CartesianGrid, Tooltip, ResponsiveContainer, PieChart, Pie, Cell, Legend } from 'recharts';

interface ProgressViewProps {
  books: Book[];
  onBack: () => void;
}

export function ProgressView({ books, onBack }: ProgressViewProps) {
  // Books read per month
  const readBooks = books.filter(b => b.status === 'read');
  
  const monthlyData = readBooks.reduce((acc, book) => {
    if (book.dateFinished) {
      const date = new Date(book.dateFinished);
      const monthYear = ⁠ ${date.getMonth() + 1}/${date.getFullYear()} ⁠;
      acc[monthYear] = (acc[monthYear] || 0) + 1;
    }
    return acc;
  }, {} as Record<string, number>);

  const chartData = Object.entries(monthlyData)
    .map(([month, count]) => ({ month, count }))
    .sort((a, b) => {
      const [monthA, yearA] = a.month.split('/').map(Number);
      const [monthB, yearB] = b.month.split('/').map(Number);
      return yearA - yearB || monthA - monthB;
    })
    .slice(-6); // Last 6 months

  // Genre distribution
  const genreData = books.reduce((acc, book) => {
    acc[book.genre] = (acc[book.genre] || 0) + 1;
    return acc;
  }, {} as Record<string, number>);

  const pieData = Object.entries(genreData).map(([genre, count]) => ({
    name: genre,
    value: count
  }));

  const COLORS = ['#f59e0b', '#3b82f6', '#10b981', '#8b5cf6', '#ef4444', '#ec4899', '#14b8a6', '#f97316'];

  return (
    <div className="max-w-6xl mx-auto">
      <button
        onClick={onBack}
        className="mb-6 flex items-center gap-2 text-black hover:text-gray-700 transition-colors"
        style={{ fontWeight: 600 }}
      >
        <ArrowLeft className="size-4" />
        Zurück zum Dashboard
      </button>

      <h1 className="text-black mb-8" style={{ fontWeight: 800 }}>Lesefortschritt & Statistiken</h1>

      {/* Stats Cards */}
      <div className="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <div className="flex items-center gap-3 mb-3">
            <div className="p-3 bg-gray-100 rounded-lg">
              <BookOpen className="size-6 text-black" />
            </div>
            <div>
              <p className="text-gray-600">Gelesene Bücher</p>
              <p className="text-gray-900" style={{ fontWeight: 800, fontSize: '1.5rem' }}>{readBooks.length}</p>
            </div>
          </div>
        </div>

        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <div className="flex items-center gap-3 mb-3">
            <div className="p-3 bg-gray-100 rounded-lg">
              <TrendingUp className="size-6 text-black" />
            </div>
            <div>
              <p className="text-gray-600">Gesamt Bücher</p>
              <p className="text-gray-900" style={{ fontWeight: 800, fontSize: '1.5rem' }}>{books.length}</p>
            </div>
          </div>
        </div>

        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <div className="flex items-center gap-3 mb-3">
            <div className="p-3 bg-gray-100 rounded-lg">
              <Award className="size-6 text-black" />
            </div>
            <div>
              <p className="text-gray-600">Verschiedene Genres</p>
              <p className="text-gray-900" style={{ fontWeight: 800, fontSize: '1.5rem' }}>{Object.keys(genreData).length}</p>
            </div>
          </div>
        </div>
      </div>

      {/* Charts */}
      <div className="grid grid-cols-1 lg:grid-cols-2 gap-6">
        {/* Monthly Reading Chart */}
        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <h2 className="text-black mb-6" style={{ fontWeight: 800 }}>Gelesene Bücher pro Monat</h2>
          {chartData.length > 0 ? (
            <ResponsiveContainer width="100%" height={300}>
              <BarChart data={chartData}>
                <CartesianGrid strokeDasharray="3 3" stroke="#f3f4f6" />
                <XAxis dataKey="month" stroke="#6b7280" />
                <YAxis stroke="#6b7280" allowDecimals={false} />
                <Tooltip
                  contentStyle={{ backgroundColor: '#fff', border: '1px solid #e5e7eb', borderRadius: '8px' }}
                />
                <Bar dataKey="count" fill="#000000" radius={[8, 8, 0, 0]} />
              </BarChart>
            </ResponsiveContainer>
          ) : (
            <div className="h-[300px] flex items-center justify-center text-gray-500">
              Noch keine Daten verfügbar
            </div>
          )}
        </div>

        {/* Genre Distribution */}
        <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200">
          <h2 className="text-black mb-6" style={{ fontWeight: 800 }}>Genre-Verteilung</h2>
          {pieData.length > 0 ? (
            <ResponsiveContainer width="100%" height={300}>
              <PieChart>
                <Pie
                  data={pieData}
                  cx="50%"
                  cy="50%"
                  labelLine={false}
                  label={({ name, percent }) => ⁠ ${name} (${(percent * 100).toFixed(0)}%) ⁠}
                  outerRadius={80}
                  fill="#8884d8"
                  dataKey="value"
                >
                  {pieData.map((entry, index) => (
                    <Cell key={⁠ cell-${index} ⁠} fill={COLORS[index % COLORS.length]} />
                  ))}
                </Pie>
                <Tooltip />
              </PieChart>
            </ResponsiveContainer>
          ) : (
            <div className="h-[300px] flex items-center justify-center text-gray-500">
              Noch keine Daten verfügbar
            </div>
          )}
        </div>
      </div>

      {/* Genre List */}
      <div className="bg-white rounded-xl p-6 shadow-sm border border-gray-200 mt-6">
        <h2 className="text-black mb-4" style={{ fontWeight: 800 }}>Deine Genres</h2>
        <div className="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-4">
          {Object.entries(genreData)
            .sort((a, b) => b[1] - a[1])
            .map(([genre, count], index) => (
              <div
                key={genre}
                className="p-4 rounded-lg border-2 border-gray-200 hover:border-black transition-colors"
              >
                <p className="text-gray-900 mb-1" style={{ fontWeight: 700 }}>{genre}</p>
                <p className="text-gray-600 text-sm">{count} {count === 1 ? 'Buch' : 'Bücher'}</p>
              </div>
            ))}
        </div>
        {Object.keys(genreData).length === 0 && (
          <p className="text-gray-500 text-center py-8">Noch keine Genres vorhanden</p>
        )}
      </div>
    </div>
  );
}
